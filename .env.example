# Server
DATABASE_URL=postgres://slang:slang@localhost:5802/slang
# openssl rand -hex 32
JWT_SECRET=change-me
PORT=5801
# Invitations email via Resend (emails are skipped when empty)
RESEND_API_KEY=
EMAIL_FROM=Slang <onboarding@resend.dev>
# Base URL used in invitation emails: the web UI (5800 in dev, your public domain in prod)
PUBLIC_URL=http://localhost:5800
